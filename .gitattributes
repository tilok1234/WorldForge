* text=auto eol=lf
*.zip binary
*.png binary
*.aseprite binary

# Pinned TileForge package fixtures must keep their exact imported bytes;
# the dependency lock hashes them (tileforge.lock.json contentSha256).
fixtures/tileforge-packages/** -text
