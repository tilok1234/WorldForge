# The pinned package copy and the resolved world (both recreated by
# node dist/tools/godot-consumer.js) and Godot's import cache. Only
# WorldForge-authored scripts and scenes are committed.
.godot/
tileforge/
world/
